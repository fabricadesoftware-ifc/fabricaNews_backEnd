[tool.pdm]

[project]
name = "fabricaNews_backEnd"
version = "0.1.0"
description = ""
authors = [
    {name = "dudubarbaro", email = "eduardobarbaro.ifc@gmail.com"},
]
dependencies = [
    "django>=4.2",
]
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-pep517>=1.0"]
build-backend = "pdm.pep517.api"
